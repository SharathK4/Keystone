# Keystone — first page

One page. A masthead and four panels that read in order:

**the network → what breaks → where capital works → the offer**

— which is the product's argument: *understand → see → decide → act*.

**Two layers, and the split is strict.** The landing layer is four product
cards: a title, one line of plain English, and one rich hero tile. It carries no
metrics at all — the single exception is the amount on the contract card,
because an offer with no amount is not an offer. Clicking a card opens the
system behind it without navigating away.

Each card's hero is a `TexturedTile`: one construction, four colourways —
forest, slate, bronze, ember. The surface is generated by `feTurbulence` warped
through a per-tone colour matrix, so it is organic rather than a gradient. The
contract card is the one the others are modelled on; giving all four a tile is
what stops three of them reading as diagrams on paper while the fourth reads as
a product. The composition stays where it is, recedes, and the
analysis comes forward on top of it, so closing returns you exactly where you
were. The four-panel grid is the anchor and never goes away. The open analysis
is reflected in the URL hash (`#network`, `#exposure`, `#intervention`,
`#offer`) so an analysis can be linked to — the hash only seeds the state, the
transition is the same.

It is a presentation layer and nothing else. Every figure on the page is a
backend response; there is no business logic, no fallback data, and no cached
"last known" number. If the analytical service is down the page says so and
renders nothing else.

## Run it

The backend must be up first — the page has nothing to show without it.

```bash
cd .. && PYTHONPATH=src python -m lce.cli serve --host 127.0.0.1 --port 8000
```

```bash
npm install && npm run dev
```

Then open <http://localhost:5173>. Vite proxies `/api` to `127.0.0.1:8000`; set
`LCE_API` to point somewhere else.

```bash
npm run build && npm run lint
```

## What comes from where

| Panel | Endpoints |
| --- | --- |
| The network | `/network`, `/network/merchants`, `/network/merchants/{id}`, `/network/dependencies`, `/network/systemic-importance` |
| What breaks | `/scenarios`, `/scenarios/{id}` |
| Where capital works | `/scenarios/{id}` (`recommended_intervention`, `alternatives`, `counterfactual`, `offer`), `/network/merchants`, `/network/systemic-importance`, `/execution/status` |
| The offer | `/scenarios/{id}` (`offer`), `/execution/status` |
| Method (inside 03) | `/scenarios/{id}` (`provenance`, `computed_in_ms`) |

`src/api/client.ts` is the only place the app touches the network. Every GET is
cached by URL for the life of the page and in-flight requests are shared,
because the backend serves a precomputed snapshot: the answer for a URL cannot
change while the tab is open. Re-selecting a scenario you have already viewed
costs nothing.

## Four decisions worth knowing

**The network is a chord diagram, not a force graph.** Merchants sit on one
ring ordered by tier, with systemic importance leading each tier band and
setting node area; dependencies are chords bundled by angular span. A force
layout at this size is a hairball, and an earlier version that placed nodes
*inside* the ring by importance was one too — with the interior occupied, 216
chords had nowhere to go. Emptying the interior is what made it readable.

**Nothing animates to imply intelligence.** No pulsing halo, no blinking origin
marker, no loading pulse, no particle field. The only motion on the page is a
sub-pixel drift on a 24–30s cycle in the network figure, and the light column
above the wordmark on a 46–58s cycle. Neither ever moves enough to track.

**The exposure figure is one grid of a hundred dots.** Ten by ten, ordered so
the three states form contiguous runs, with the counts in a legend beside it.
It went through two worse designs first: a tick spectrum that encoded three
quantities and communicated none of them, and a three-block unit chart at three
different scales. Both were cleverer. Neither let you see one-in-a-hundred at a
glance, which is the only thing this figure has to do.

**Leverage is measured, not predicted.** Panel 3's headline is
`counterfactual.disruption_prevented / counterfactual.cost` — the replayed
before/after of the whole selected plan. The per-action `×` figures in the
comparison are each action's own leverage measured alone, which is a different
denominator; the axis says so, because the two numbers differ and an unlabelled
pair reads as a contradiction.

## States the page has to handle, and does

- **Three of seven scenarios carry no recommendation and no offer.** Panels 3
  and 4 say which precondition failed and show the probed provider
  capabilities. They do not invent an offer.
- **Every alternative in this snapshot prevents exactly ₹0.** The comparison
  draws that as a mark on the floor rather than hiding it. Only the selected
  plan moved the objective, and that is the finding.
- **Route / Direct Transfers are not enabled** on the test account, so the offer
  card footer reads "Test capability unavailable — recorded as a plan, no funds
  move". It never implies an execution happened.
- **Offers are scenario-specific.** Every offer in this snapshot is a
  `liquidity_injection`, but the *reason* differs completely between a missed
  inflow and a supplier failure — so `lib/offerCopy.ts` keys on scenario family
  first and intervention type second, and the card carries one of four accent
  treatments. Keying on type alone made all seven scenarios read identically.
- **Some merchants have no estimated dependency at all.** Hovering one rings it
  rather than dimming the other ninety-nine to nothing.
- **Backend down** → "Analytical service unavailable", the failing path, and the
  command to start it.

## React Bits

`src/components/reactbits/` — `SpecularButton` and `LightColumn`, implemented
locally against the published patterns rather than installed. Both are
CSS-driven: pointer position is written to custom properties, so a highlight
follows the cursor without re-rendering the tree.

Used sparingly and on purpose. The specular button is the offer's primary
action, and the only one on the page. `LightColumn` is a single soft shaft
descending onto the wordmark — architectural rather than atmospheric, one wide
monochrome gradient with a narrow core and a pool where it lands, drifting a few
pixels on a 46-second cycle, and paused entirely while an analysis is open. The
earlier side-ray beams were removed: at the offer card's proportions they
resolved into a straight vertical seam that read as a rendering artefact rather
than as light.

`ReflectiveCard` was removed. `RazorpayContractCard` and `TexturedTile` between
them do the job it was written for — a generated surface with its own highlight
— and keeping a second, unused implementation of the same idea in the tree was
not worth the file.

## Units: what is rupees and what is not

The one place this UI could quietly lie. The backend's objective `D(G,S)` sums
three deliberately different terms — a value-weighted delay term in INR-days, a
default count, and a liquidity deficit-time term in INR-hours — reconciled by
configured weights. Its docstring says so explicitly.

So these fields are an **index**, not money:

    disruption_prevented   baseline_disruption   attributable_disruption
    network_disruption_index   marginal_disruption   disruption_spread
    regret   capital_efficiency (= index per rupee)

and these are **rupees**:

    disrupted_value   commerce_preserved   baseline_disrupted_value
    downstream_delayed_value   predicted_downstream_disruption
    amount   cost   liquidity_required   proposed_amount   indicative_cost

An earlier version of this page rendered `disruption_prevented / cost` as
"₹1 → ₹20 protected". That was false: 20.5 is *index units per rupee*, which is
what the search ranks on, and the true rupee ratio for the same plan is 1.08×.
`lib/format.ts` now has a separate `idx()` formatter with no currency sign, and
every index quantity on screen is labelled as an index. The money story is told
with `commerce_preserved`.

Two related traps the same view has to avoid. `counterfactual.cost` is the
**whole plan**, while `recommended_intervention` is one action inside it — so
the plan is listed as a set of actions rather than implied to be one. And an
action's `amount` and its `cost` are different numbers: a supplier term
extension can be worth ₹16.5 L while committing ₹1,321, because it moves dates
rather than money. Both columns are shown so the rows never look like they
should sum to the plan total when they do not.

## Palette

The ground is a cool lavender gradient moving between `#F5F5FB`, `#F2F2FA` and
`#EFF0F8` — brightest under the masthead, coolest at the lower right.

The whole neutral ramp moves with it: paper, surfaces, borders, greys and
shadows are all cool. Warm neutrals on a lavender ground read as dirty rather
than as contrast, so leaving them would have been worse than not changing the
background at all. The two saturated families stay warm on purpose — terracotta
for the analytical accent, Razorpay orange for the contract — because they are
the only things on the page that should feel lit.

The light column above the wordmark is a neutral white wash with a trace of
warmth in its narrow core only. A warm wash on a cool ground reads as a yellow
stain rather than as a light source.

## Why the overlay is portalled

`ExpandedAnalysis` renders through `createPortal` into `document.body`, and the
page's receding effect uses `opacity` rather than `filter`. That is not
stylistic. `filter`, `transform` and `backdrop-filter` all make an element the
containing block for `position: fixed` descendants — so a `saturate()` on the
page made the overlay size itself to the whole document instead of the viewport,
and its lower half became unreachable. The portal makes that class of bug
impossible regardless of what any ancestor does.

## The contract card

`RazorpayContractCard` is the only saturated colour in the product and the only
place the brand appears. Its surface is generated rather than drawn: an SVG
`feTurbulence` seeded from the merchant id, so m0005's card and m0071's card are
visibly different objects while staying obviously the same product. The card
really is issued to that merchant rather than being a template with a name
dropped into it — the amount, the term, the repayment shape and the eligibility
ceiling are all what the optimiser sized for them.

The expanded contract is laid out as a product page: a line of context naming
the merchant and their obligations, a headline, the card in its own light with
the action beneath it, and four checkmarked points beside it. Every point is a
backend figure, and the "why this merchant" paragraph is the backend's own
sentence, printed verbatim.

## Fonts

The display face is **Against** (`public/fonts/against-regular.otf`), chosen
over the other two files in `../fonts/` on inspection: *Emotions Condensed
Demo* substitutes "TRIAL FONT / 177studio.com" watermark glyphs into its digits
and letters, and *Wolfgang* is a 94-glyph blackletter.

Two things to know. Against is **licensed for personal use only** — a
commercial release needs a licence from 177studio, and the face is swappable
via `--display` in `src/styles/tokens.css`. And it has no `₹`, no arrows and
proportional digits, so it is confined to the wordmark and panel titles;
**every figure is set in the text stack with tabular numerals** so columns line
up and a changing value does not reflow its neighbours.

## Layout

Desktop is the target: a true 2×2 of four identical panels (measured 634×550
each at 1400px wide). Panel heads are locked to one height because they carry
different asides — a legend, a seven-way selector, a badge — and letting each
size itself put the rules across the composition out of register.

Panels are frosted rather than glassy: a translucent paper surface with a 7px
blur, so the light column behind them registers and nothing more.

Below 1080px the grid becomes a vertical sequence with the panels keeping their
height floor and internal hierarchy. Verified free of horizontal overflow at
430, 805 and 1385 px, on the landing composition and inside an open analysis.
